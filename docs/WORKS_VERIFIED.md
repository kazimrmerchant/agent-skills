# Works-verified

**Rate these first.** This is the recommended install/rate set: twelve unique skills with a numbered procedure an agent can execute (real files, commands, or APIs).

The rest of the uniqueness inventory is **untested copy** — not a claim those bodies fail, and not a claim they work.

Do not pad toward 500. Do not vendor `last30days`.

## The 12

| Skill | Tag |
|-------|-----|
| `better` | agent-os |
| `godot-ui` | godot |
| `emil-design-eng` | frontend |
| `stable-diffusion` | ai-ml |
| `weights-and-biases` | ai-ml |
| `github-actions-advanced` | git-github |
| `git-workflow` | agent-os |
| `docker-management` | devops |
| `ollama-local-setup` | ai-ml |
| `threejs-fundamentals` | threejs |
| `chroma` | ai-ml |
| `mcp-server-authoring` | agent-os |

Preferred install: `npx skills add kazimrmerchant/skill-slice --skill better` (swap `better` for any name below).

```powershell
Copy-Item -Recurse .\skills\<name> $env:USERPROFILE\.cursor\skills\
```

Sample evidence (12 overlay WORKS, 5 overlay WEAK, 5 extra sample WORKS in INCLUSION only, 2 FAIL cut): [INCLUSION.md](INCLUSION.md#works-bar-executability).
