# Transition components

Canonical Remotion implementations live in each pack, e.g.:

`<pack>/remotion/src/components/transitions/`

- `SceneEffects.tsx` — GlitchOverlay, FilmBurnOverlay, PunchZoomStill
- Prefer `@remotion/transitions` `TransitionSeries` + `fade` / `slide` / `wipe` for cuts

See parent `SKILL.md` for pacing and mix rules.
